"use client"
import { ArrowRight, Sparkles } from "lucide-react"
import { useRouter } from "next/navigation"
export default function NextActionCard({ nextAction }) { const router=useRouter(); return <section className="relative overflow-hidden rounded-2xl border border-primary/20 bg-primary p-6 text-primary-foreground shadow-sm"><div className="absolute -right-12 -top-12 size-40 rounded-full bg-white/10 blur-2xl"/><div className="relative"><Sparkles className="size-5"/><p className="mt-5 text-xs font-semibold uppercase tracking-[0.16em] text-primary-foreground/70">Next best action</p><h2 className="mt-2 text-xl font-semibold">{nextAction?.title ?? "Keep progressing"}</h2><p className="mt-2 text-sm leading-6 text-primary-foreground/75">{nextAction?.description ?? "Continue building evidence toward your career goal."}</p>{nextAction?.href && <button onClick={()=>router.push(nextAction.href)} className="mt-5 inline-flex items-center gap-2 rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-primary">Continue <ArrowRight className="size-4"/></button>}</div></section> }
