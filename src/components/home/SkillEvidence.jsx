"use client"
import { Bar, BarChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts"
import { BadgeCheck } from "lucide-react"
export default function SkillEvidence({ evidence }) { const claimed=evidence?.claimedSkills?.length ?? 0, demonstrated=evidence?.demonstratedSkills?.length ?? 0; const data=[{name:"Claimed",value:claimed},{name:"Demonstrated",value:demonstrated}]
 return <section className="rounded-2xl border border-border bg-card p-6 shadow-sm shadow-black/[0.02]"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Skill Evidence</h2><p className="mt-1 text-xs text-muted-foreground">Claimed vs proven skills</p></div><BadgeCheck className="size-5 text-primary"/></div><div className="mt-4 h-40"><ResponsiveContainer width="100%" height="100%"><BarChart data={data} layout="vertical" margin={{left:0,right:18}}><XAxis type="number" allowDecimals={false} hide/><YAxis type="category" dataKey="name" width={90} tickLine={false} axisLine={false} tick={{fill:"var(--muted-foreground)",fontSize:11}}/><Tooltip cursor={{fill:"var(--muted)"}} contentStyle={{background:"var(--card)",border:"1px solid var(--border)",borderRadius:12,fontSize:12}}/><Bar dataKey="value" fill="var(--chart-2)" radius={[0,7,7,0]} barSize={18}/></BarChart></ResponsiveContainer></div><div className="flex flex-wrap gap-2">{evidence?.demonstratedSkills?.slice(0,4).map((s,i)=><span key={`${s}-${i}`} className="rounded-full bg-primary/10 px-2.5 py-1 text-[11px] font-medium text-primary">{s}</span>)}{demonstrated===0 && <p className="text-xs text-muted-foreground">Complete assessments and interviews to demonstrate your skills.</p>}</div></section> }
