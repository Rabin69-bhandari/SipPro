"use client"
import { ArrowRight, Mic } from "lucide-react"
import { useRouter } from "next/navigation"
import { Bar, BarChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts"
export default function InterviewCard({ interview, stats, readiness }) { const router=useRouter(); if(!interview)return <section className="rounded-2xl border border-border bg-card p-6"><div className="flex items-center justify-between"><div><h2 className="font-semibold">AI Interview</h2><p className="mt-1 text-xs text-muted-foreground">No interview evidence yet</p></div><Mic className="size-5 text-primary"/></div><p className="mt-5 text-sm leading-6 text-muted-foreground">Practice technical knowledge, problem solving, communication and practical reasoning.</p><button onClick={()=>router.push('/interview/practice')} className="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-primary">Start interview <ArrowRight className="size-4"/></button></section>
 const data=[{name:"Technical",score:interview.technicalScore ?? 0},{name:"Problem",score:interview.problemSolvingScore ?? 0},{name:"Communication",score:interview.communicationScore ?? 0},{name:"Practical",score:interview.practicalReasoningScore ?? 0}]
 return <section className="rounded-2xl border border-border bg-card p-6 shadow-sm shadow-black/[0.02]"><div className="flex items-start justify-between"><div><h2 className="font-semibold">Interview Performance</h2><p className="mt-1 text-xs text-muted-foreground">Latest interview • {stats?.attempts ?? 0} attempt{(stats?.attempts ?? 0)===1?"":"s"}</p></div><div className="text-right"><p className="text-2xl font-semibold">{readiness?.interview ?? interview.totalScore ?? 0}%</p><p className="text-[11px] text-muted-foreground">Readiness contribution</p></div></div><div className="mt-5 h-52"><ResponsiveContainer width="100%" height="100%"><BarChart data={data} layout="vertical" margin={{left:5,right:10}}><XAxis type="number" domain={[0,25]} hide/><YAxis type="category" dataKey="name" width={95} tickLine={false} axisLine={false} tick={{fill:"var(--muted-foreground)",fontSize:10}}/><Tooltip cursor={{fill:"var(--muted)"}} contentStyle={{background:"var(--card)",border:"1px solid var(--border)",borderRadius:12,fontSize:12}} formatter={(v)=>[`${v}/25`,"Score"]}/><Bar dataKey="score" fill="var(--chart-2)" radius={[0,7,7,0]} barSize={15}/></BarChart></ResponsiveContainer></div></section> }
