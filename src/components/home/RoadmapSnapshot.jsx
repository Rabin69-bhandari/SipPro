"use client"
import { ArrowRight, BookOpen, CheckCircle2 } from "lucide-react"
import { useRouter } from "next/navigation"
export default function RoadmapSnapshot({ roadmap }) { const router=useRouter(); if(!roadmap)return <section className="rounded-2xl border border-border bg-card p-6"><h2 className="font-semibold">Learning Roadmap</h2><p className="mt-4 text-sm text-muted-foreground">No roadmap created yet.</p></section>
 const completed=roadmap.completedTopics ?? 0,total=roadmap.totalTopics ?? 0,progress=roadmap.progress ?? 0
 return <section className="rounded-2xl border border-border bg-card p-6 shadow-sm shadow-black/[0.02]"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2"><BookOpen className="size-4 text-primary"/><h2 className="font-semibold">Roadmap Progress</h2></div><p className="mt-1 max-w-xl text-sm text-muted-foreground">{roadmap.title}</p></div><span className="text-2xl font-semibold text-primary">{progress}%</span></div><div className="mt-6 h-3 overflow-hidden rounded-full bg-secondary"><div className="h-full rounded-full bg-primary transition-all duration-700" style={{width:`${progress}%`}}/></div><div className="mt-3 flex items-center justify-between text-xs text-muted-foreground"><span className="inline-flex items-center gap-1.5"><CheckCircle2 className="size-3.5 text-primary"/>{completed} completed</span><span>{total} total topics</span></div><div className="mt-6 grid gap-2 sm:grid-cols-2">{roadmap.modules?.slice(0,4).map((m,i)=><div key={m.id ?? i} className="flex items-center gap-3 rounded-xl bg-secondary/45 p-3"><span className="flex size-7 shrink-0 items-center justify-center rounded-lg bg-card text-xs font-semibold">{i+1}</span><div className="min-w-0"><p className="truncate text-xs font-medium">{m.title}</p><p className="mt-0.5 text-[11px] text-muted-foreground">{m.topics?.length ?? 0} topics</p></div></div>)}</div><button onClick={()=>router.push(`/roadmap/${roadmap.id}`)} className="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-primary">View full roadmap <ArrowRight className="size-4"/></button></section> }
